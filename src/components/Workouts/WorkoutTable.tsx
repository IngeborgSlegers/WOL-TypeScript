import * as React from 'react';

export interface WorkoutTableProps {
  
}
 
const WorkoutTable: React.SFC<WorkoutTableProps> = () => {
  return ( 
    <h1>WorkoutTable is connected!</h1>
   );
}
 
export default WorkoutTable;